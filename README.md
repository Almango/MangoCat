# MangoCat - Astro博客主题

一个基于Astro构建的现代化、优雅的博客主题，简洁而不失美感，专注于提供良好的阅读体验。

## ✨ 特性

- 🚀 **高性能**: 基于Astro的静态站点生成，提供极速加载体验
- 📱 **响应式设计**: 完美适配各种屏幕尺寸，从手机到桌面设备
- 🎨 **优雅的UI设计**: 简洁、现代的设计风格，专注于阅读体验
- ✨ **动画效果**: 集成animate.css提供流畅的动画效果
- 📝 **内容管理**: 使用Astro Content Collections轻松管理博客文章
- 📊 **字数统计与阅读时间**: 自动计算每篇文章的字数和预计阅读时间
- 🏷️ **分类与标签**: 支持文章分类和标签功能
- 📅 **日期格式化**: 美观的日期显示格式

## 🛠️ 技术栈

- **前端框架**: Astro
- **样式**: CSS3
- **动画**: animate.css
- **内容管理**: Astro Content Collections
- **图标**: SVG图标
- **构建工具**: Vite

## 📁 项目结构

```text
/
├── public/                 # 静态资源目录
│   ├── avatar.png          # 头像图片
│   └── favicon.svg         # 网站图标
├── src/
│   ├── assets/             # 项目资源
│   │   ├── fonts/          # 字体文件
│   │   └── images/         # 图片资源
│   ├── components/         # 可复用组件
│   │   ├── Header.astro    # 页头组件
│   │   └── Postlist.astro  # 文章列表组件
│   ├── config.ts           # 网站配置文件
│   ├── content/            # 内容集合
│   │   ├── config.ts       # 内容配置
│   │   └── posts/          # 博客文章目录
│   ├── layouts/            # 布局组件
│   │   └── Layout.astro    # 主布局组件
│   ├── pages/              # 页面组件
│   │   ├── index.astro     # 首页
│   │   ├── posts/          # 文章页面
│   │   │   └── [slug].astro # 文章详情页模板
│   │   └── styles/         # 样式文件
│   └── utils/              # 工具函数
│       └── Readtime-Wordcount.ts # 阅读时间和字数统计工具
├── .gitignore              # Git忽略文件
├── astro.config.mjs        # Astro配置文件
├── package.json            # 项目依赖
├── tsconfig.json           # TypeScript配置
└── README.md               # 项目说明文档
```

## 🚀 快速开始

### 1. 安装依赖

```sh
npm install
```

或者使用pnpm:

```sh
pnpm install
```

### 2. 启动开发服务器

```sh
npm run dev
```

或者使用pnpm:

```sh
pnpm run dev
```

服务器将在 `http://localhost:4321` 启动。

### 3. 构建生产版本

```sh
npm run build
```

或者使用pnpm:

```sh
pnpm run build
```

构建产物将输出到 `./dist/` 目录。

### 4. 预览生产版本

```sh
npm run preview
```

或者使用pnpm:

```sh
pnpm run preview
```

## 📝 内容管理

### 添加新文章

在 `src/content/posts/` 目录下创建新的Markdown文件，文章格式如下：

```markdown
---
title: "文章标题"
description: "文章描述"
published: "2023-01-01"
category: "分类名称"
tags: ["标签1", "标签2"]
---

这里是文章内容...
```

### 文章元数据说明

- `title`: 文章标题
- `description`: 文章描述
- `published`: 发布日期，格式为YYYY-MM-DD
- `category`: 文章分类
- `tags`: 文章标签，数组格式

## ⚙️ 自定义配置

### 网站配置

在 `src/config.ts` 文件中可以修改网站的基本配置：

```typescript
export const siteConfig = {
  title: 'Almango',          // 网站标题
  author: 'MangoCat',        // 作者名称
  favicon: '/favicon.svg',   // 网站图标
  desc: '天真永不消逝，浪漫至死不渝', // 网站描述
  siteUrl: 'https://blog.almango.cn', // 网站URL
}

export const profileConfig = {
  name: 'Almango',           // 个人名称
  avatar: '/avatar.png',     // 头像图片
  desc: '天真永不消逝，浪漫至死不渝', // 个人描述
}
```

### 修改样式

网站样式文件位于 `src/styles/` 目录下：

- `global.css`: 全局样式
- `markdown.css`: Markdown内容样式

## 📦 部署

### Vercel

1. 登录Vercel
2. 点击"New Project"
3. 导入你的GitHub仓库
4. 选择Astro作为框架
5. 点击"Deploy"部署项目

### Netlify

1. 登录Netlify
2. 点击"New site from Git"
3. 导入你的GitHub仓库
4. 构建命令: `npm run build`
5. 发布目录: `dist`
6. 点击"Deploy site"部署项目

### GitHub Pages

1. 修改 `astro.config.mjs` 文件，添加GitHub Pages配置
2. 运行 `npm run build` 构建项目
3. 推送构建产物到GitHub仓库的 `gh-pages` 分支

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交Issue和Pull Request！
